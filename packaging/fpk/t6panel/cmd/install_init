#!/bin/bash
. "$(dirname "$0")/common"
[ -d /sys/module/t6_platform ] || fail "Install the T6 Drivers package first."
[ -x /usr/sbin/t6-ledd ] || fail "Install T6 Control Center first (it provides the fan and LED daemons)."
exit 0
